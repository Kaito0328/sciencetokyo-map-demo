# Rename to .env.local and set your credentials (do NOT commit .env.local)
BASIC_AUTH_USER=your-user
BASIC_AUTH_PASS=your-strong-password
# Optional realm name shown in the browser prompt
BASIC_AUTH_REALM=ScienceTokyoMap
 